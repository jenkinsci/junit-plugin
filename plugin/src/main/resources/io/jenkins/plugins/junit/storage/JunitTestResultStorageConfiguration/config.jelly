<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">
  <j:if test="${descriptor.descriptors.size() gt 1}">
    <f:section title="${%Junit}">
      <f:entry>
        <f:dropdownDescriptorSelector field="storage" title="Test result storage" descriptors="${descriptor.descriptors}"/>
      </f:entry>
    </f:section>
  </j:if>
</j:jelly>
